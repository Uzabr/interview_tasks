package algo_task_2;

public class BestSaleDay {

    private static int saleDay(int[] prices) {
        if (prices == null || prices.length == 0) return 0;
        int startPrice = prices[0];
        int profit = 0;

        for (int i =0; i < prices.length; i++) {
            
        }
    }
}
